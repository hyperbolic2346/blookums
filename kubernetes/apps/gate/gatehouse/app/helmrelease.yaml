---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gatehouse
  namespace: gate
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: gatehouse
  values:
    controllers:
      main:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          main:
            image:
              repository: ghcr.io/hyperbolic2346/gatehouse
              tag: aa87f9b8e83dcd5159fc63f1b37cd4f3350926b2@sha256:924cc7e42b7706bac757e750c1adc6984a7aae4afc481ca82df43f7aff1ae685
            env:
              PORT: "8080"
              FRONTEND_DIR: /app/frontend-dist
              FRIGATE_URL: http://frigate.home.svc.cluster.local:5000
              MQTT_BROKER: tcp://mosquitto.home.svc.cluster.local:8883
              DB_PATH: /data/gatehouse.db
            envFrom:
              - secretRef:
                  name: gatehouse-secret
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: 8080
                  initialDelaySeconds: 5
                  periodSeconds: 30
                  timeoutSeconds: 3
                  failureThreshold: 3
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: 8080
                  initialDelaySeconds: 2
                  periodSeconds: 10
                  timeoutSeconds: 3
                  failureThreshold: 3
            resources:
              requests:
                cpu: 50m
                memory: 64Mi
              limits:
                memory: 256Mi

    service:
      main:
        controller: main
        ports:
          http:
            port: 8080

    route:
      main:
        hostnames: ["gatehouse.${SECRET_DOMAIN}"]
        parentRefs:
          - name: external
            namespace: kube-system
            sectionName: https
        rules:
          - backendRefs:
              - identifier: main
                port: 8080

    persistence:
      data:
        enabled: true
        existingClaim: gatehouse-data
        globalMounts:
          - path: /data
