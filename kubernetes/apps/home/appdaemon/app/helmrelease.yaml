---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: appdaemon
  namespace: home
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: appdaemon
  values:
    controllers:
      main:
        containers:
          main:
            image:
              repository: docker.io/acockburn/appdaemon
              tag: 4.5.13
            env:
              TZ: ${TIMEZONE}
              ELEVATION: ${SECRET_ALTITUDE}
              HA_URL: http://home-assistant:8123
              DASH_URL: http://0.0.0.0:5050
              # /conf/apps is a read-only ConfigMap mount; don't try to write
              # __pycache__ there.
              PYTHONDONTWRITEBYTECODE: "1"
            envFrom:
              - secretRef:
                  name: appdaemon-secret
    service:
      main:
        controller: main
        ports:
          http:
            port: 5050

    route:
      main:
        hostnames: ["appdaemon.${SECRET_DOMAIN}"]
        parentRefs:
          - name: internal
            namespace: kube-system
            sectionName: https
        rules:
          - backendRefs:
              - identifier: main
                port: 5050
    persistence:
      conf:
        enabled: true
        existingClaim: appdaemon-config
        globalMounts:
          - path: /conf
      # Apps come from git (kustomize configMapGenerator over ./apps), mounted
      # over the PVC's old /conf/apps copies. Push to main to deploy; AppDaemon
      # hot-reloads on the file change.
      apps:
        type: configMap
        name: appdaemon-apps
        globalMounts:
          - path: /conf/apps

    addons:
      code:
        enabled: true
        image:
          repository: codercom/code-server
          tag: 4.129.0
        args:
          - --auth
          - none
          - --user-data-dir
          - /conf/.vscode
          - --extensions-dir
          - /conf/.vscode
        workingDir: /conf
        ingress:
          enabled: true
          annotations:
            kubernetes.io/tls-acme: 'true'
            kubernetes.io/ingress.class: nginx
            nginx.ingress.kubernetes.io/auth-url: http://oauth2-proxy.networking.svc.cluster.local/oauth2/auth
            nginx.ingress.kubernetes.io/auth-signin: ${SECRET_AUTH_URL}
            cert-manager.io/cluster-issuer: letsencrypt-production
          hosts:
            - host: appdaemon-code.${SECRET_DOMAIN}
              paths:
                - path: /
                  pathType: Prefix
          tls:
            - hosts:
                - appdaemon-code.${SECRET_DOMAIN}
              secretName: hass-cert
        volumeMounts:
          - name: conf
            mountPath: /conf
          # Same read-only apps view as AppDaemon — edits to apps go through
          # git now.
          - name: apps
            mountPath: /conf/apps
